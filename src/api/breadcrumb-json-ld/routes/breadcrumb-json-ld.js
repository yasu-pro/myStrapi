'use strict';

/**
 * breadcrumb-json-ld router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::breadcrumb-json-ld.breadcrumb-json-ld');
